import * as React from "react";
import {SFC} from "react";
import * as ReactDOM from "react-dom";


const Options: SFC<{}> = ({}) => {
    return <div>
    
    </div>;
};

export const reactMain = function(): void {
    ReactDOM.render(<Options/>, document.body.appendDiv());
};